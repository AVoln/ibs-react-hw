import { styled } from '@mui/material';

export const ImgWrapper = styled('div')((_) => {
  return {
    maxWidth: '6.36rem',
    margin: '0 auto',
    paddingTop: '2rem',
    textAlign: 'center',
  };
});
