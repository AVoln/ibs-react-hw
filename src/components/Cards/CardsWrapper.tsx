import { styled } from '@mui/material';

export const CardsWrapper = styled('section')((_) => {
  return {
    display: 'flex',
    paddingBottom: '1.71rem',
    gap: '1.14rem',
    flexWrap: 'wrap',
    justifyContent: 'center',
  };
});
