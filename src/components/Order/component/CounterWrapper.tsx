import { styled } from '@mui/system';

export const CounterWrapper = styled('div')((_) => {
  return {
    display: 'flex',
    lineHeight: '2.71rem',
    transition: 'color 0.1s ease-in-out',
  };
});
