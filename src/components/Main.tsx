import { Cards } from './Cards';
import { Modal } from './Modal';

export const Main = () => {
  return (
    <main className='catalog'>
      <Cards></Cards>
      <Modal></Modal>
    </main>
  );
};
